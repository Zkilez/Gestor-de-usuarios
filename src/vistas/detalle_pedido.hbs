<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del Pedido - DIAGONAL</title>
    <style>
        :root {
            --primary-color: #007bff;
            --primary-dark: #0056b3;
            --secondary-color: #6c757d;
            --background-color: #f8f9fa;
            --text-color: #333;
            --white: #ffffff;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://img.freepik.com/foto-gratis/grupo-personas-diversas-trabajo-equipo-manos-unidas_53876-146432.jpg') no-repeat center center;
            background-size: cover;
            filter: brightness(30%);
            z-index: -1;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .header {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            width: 100%;
            text-align: center;
        }

        .header h1 {
            color: var(--primary-color);
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }

        .content {
            width: 100%;
            max-width: 600px;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            backdrop-filter: blur(10px);
        }

        .card h2 {
            color: var(--primary-color);
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .detail-group {
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .detail-label {
            font-weight: bold;
            color: var(--secondary-color);
        }

        .detail-value {
            text-align: right;
        }

        .btn {
            display: inline-block;
            background-color: var(--primary-color);
            color: var(--white);
            text-decoration: none;
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-align: center;
            margin-top: 1.5rem;
        }

        .btn:hover {
            background-color: var(--primary-dark);
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }

            .card {
                padding: 1.5rem;
            }

            .detail-group {
                flex-direction: column;
                align-items: flex-start;
            }

            .detail-value {
                text-align: left;
                margin-top: 0.25rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>DIAGONAL</h1>
            <p>Sistema de Gestión de Pedidos</p>
        </header>
        <main class="content">
            <div class="card">
                <h2>Detalles del Pedido</h2>
                <div class="detail-group">
                    <span class="detail-label">ID Pedido:</span>
                    <span class="detail-value">{{pedido.id_pedido}}</span>
                </div>
                <div class="detail-group">
                    <span class="detail-label">Comprador:</span>
                    <span class="detail-value">{{pedido.comprador}}</span>
                </div>
                <div class="detail-group">
                    <span class="detail-label">Valor:</span>
                    <span class="detail-value">{{pedido.valor}}</span>
                </div>
                <div class="detail-group">
                    <span class="detail-label">Artículo:</span>
                    <span class="detail-value">{{pedido.articulo}}</span>
                </div>
                <div class="detail-group">
                    <span class="detail-label">Descripción:</span>
                    <span class="detail-value">{{pedido.descripcion}}</span>
                </div>
                <div class="detail-group">
                    <span class="detail-label">Fecha:</span>
                    <span class="detail-value">{{pedido.date}}</span>
                </div>
                <a href="/" class="btn">Volver al inicio</a>
            </div>
        </main>
    </div>
</body>
</html>