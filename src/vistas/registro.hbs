<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DIAGONAL - Registro</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative; /* Necesario para el pseudo-elemento */
            overflow: hidden; /* Para evitar barras de desplazamiento */
        }
        .register-container {
            background-color: rgba(255, 255, 255, 0.8); /* Fondo blanco con opacidad */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 400px;
            width: 100%;
            text-align: center;
            backdrop-filter: blur(10px); /* Aplica un efecto de desenfoque */
        }
        .register-container h1 {
            margin-bottom: 20px;
            color: #000; /* Color negro */
            font-size: 36px; /* Tamaño del texto */
            font-weight: bold; /* Peso del texto */
        }
        h1.title {
            color: #fff; /* Color blanco para el título "DIAGONAL" */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6); /* Sombra de texto para mejor visibilidad */
            font-size: 36px; /* Tamaño del texto */
            font-weight: bold; /* Peso del texto */
        }
        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }
        button {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            width: 100%;
            margin-top: 20px;
        }
        button:hover {
            background-color: #0056b3;
        }
        a {
            color: #007bff;
            text-decoration: none;
            display: block;
            margin-top: 15px;
        }
        a:hover {
            text-decoration: underline;
        }
        .header {
            text-align: center;
            margin-bottom: 40px;
            position: relative; /* Para posicionar el fondo detrás del texto */
        }
        .header img {
            max-width: 150px; /* Ajusta el tamaño de la imagen según sea necesario */
            height: auto;
            border-radius: 50%; /* Esto hace que la imagen sea circular */
        }
        /* Pseudo-elemento para el filtro de color */
        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://img.freepik.com/foto-gratis/grupo-personas-diversas-trabajo-equipo-manos-unidas_53876-146432.jpg') no-repeat center center fixed;
            background-size: cover;
            filter: brightness(70%); /* Ajusta la luminosidad de la imagen para darle un tinte blanquecino */
            z-index: -1; /* Asegura que el pseudo-elemento esté detrás del contenido */
        }
    </style>
</head>
<body>
    <div class="header">
        <!-- Imagen del logo de la empresa desde el enlace directo -->
        <img src="https://us.123rf.com/450wm/vectoryzen/vectoryzen1901/vectoryzen190100931/114991370-letra-b-creativo-ilustrador-de-vector-de-plantilla-de-logotipo-elementos-aislados-vector.jpg?ver=6" alt="Logo Empresa Diagonal">
        <h1 class="title">DIAGONAL</h1> <!-- Título de la empresa -->
    </div>
    <div class="register-container">
        <h1>Registro</h1>
        <form action="/registro" method="POST">
            <input type="email" name="email" placeholder="Escribe tu email" required>
            <input type="password" name="password" placeholder="Escribe tu contraseña" required>
            <button type="submit">Crear Cuenta</button>
        </form>
        <a href="/inicioSesion">¿Ya tienes cuenta? Inicia sesión</a>
    </div>
</body>
</html>
